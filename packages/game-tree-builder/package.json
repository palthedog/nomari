{
    "name": "@nomari/game-tree-builder",
    "version": "1.0.0",
    "description": "Game tree builder for Nomari game",
    "main": "game-tree-builder.js",
    "types": "game-tree-builder.d.ts",
    "scripts": {
        "build": "tsc",
        "pretest": "npm run generate --workspace=@nomari/ts-proto && npm run build --workspace=@nomari/ts-proto",
        "test": "jest",
        "test:watch": "jest --watch",
        "lint": "eslint .",
        "lint:fix": "eslint . --fix"
    },
    "dependencies": {
        "@nomari/ts-proto": "*",
        "@nomari/game-tree": "*"
    },
    "devDependencies": {
        "@nomari/eslint-config": "*",
        "@types/jest": "^29.5.0",
        "@types/node": "^20.0.0",
        "jest": "^29.5.0",
        "ts-jest": "^29.1.0",
        "typescript": "^5.0.0"
    }
}